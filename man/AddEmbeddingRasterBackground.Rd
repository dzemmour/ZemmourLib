% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot_functions.R
\name{AddEmbeddingRasterBackground}
\alias{AddEmbeddingRasterBackground}
\title{Add a Library Raster Background to a ggplot}
\usage{
AddEmbeddingRasterBackground(
  lineage,
  interpolate = TRUE,
  alpha = 1,
  color = NULL,
  resolution = NULL
)
}
\arguments{
\item{lineage}{Character string of the cell type, options: allT, CD8, CD4, Treg, gdT, CD8aa, Tz, DN, DP
Must correspond to an .rds file in the library's extdata folder.}

\item{interpolate}{Logical; whether to interpolate the raster image.}

\item{alpha}{Transparency of the background layer.}

\item{color}{Optional; a color to tint the background points (e.g., "grey").}

\item{resolution}{Optional; a numeric vector of length 2 to resample the image.}
}
\value{
A ggplot2 \code{annotation_raster} layer.
}
\description{
Reads a pre-cached background from the ZemmourLib internal data (inst/extdata)
and adds it as a layer using \code{annotation_raster}.
}
\examples{
\dontrun{

# 1. Create your standard DimPlot
p <- Seurat::DimPlot(
  so_query,
  reduction = "mde_incremental",
  group.by = "level2",
  pt.size = 2,
  raster = FALSE
)

# 2. Generate the background layer from the library
bg <- AddEmbeddingRasterBackground(
  lineage = "CD4",
  interpolate = FALSE,
  color = "grey",
  alpha = 0.5
)

# 3. Insert the background layer BEHIND the DimPlot points
p$layers <- c(list(bg), p$layers)

# 4. Final adjustments
p + ggplot2::coord_fixed()
}
}
