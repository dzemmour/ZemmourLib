% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_functions.R
\name{MyFeatureScatter}
\alias{MyFeatureScatter}
\title{Generate Feature Scatter Plots with Highlighted Cell Density}
\usage{
MyFeatureScatter(
  so = so,
  assay = "ADT",
  slot = "data",
  feature1 = NULL,
  feature2 = NULL,
  group.by = sprintf("is_\%s", cl),
  split.by = NULL,
  raster = T,
  cols = rev(rainbow(10, end = 4/6))
)
}
\arguments{
\item{so}{A Seurat object. Default is \code{so}.}

\item{assay}{Character string, name of the assay to pull features from (e.g., "ADT", "RNA"). Default is "ADT".}

\item{slot}{Character string, data slot to use (e.g., "data", "counts", "scale.data"). Default is "data".}

\item{feature1}{Character string, name of the first feature (e.g., gene or ADT).}

\item{feature2}{Character string, name of the second feature.}

\item{group.by}{Character string, metadata column to define the highlighted group (should be logical TRUE/FALSE). Default is \code{sprintf("is_\%s", cl)}.}

\item{split.by}{Character string, metadata column to split plots by using \code{facet_wrap}. Default is NULL.}

\item{raster}{Logical, whether to use \code{geom_scattermore} for highlighted points (TRUE) or \code{geom_point} (FALSE). Default is TRUE.}

\item{cols}{A character vector of colors to define the density color ramp. Default is \code{rev(rainbow(10, end = 4/6))}.}

\item{highlight_size}{Numeric, point size for highlighted cells. Default is 1.}

\item{highlight_alpha}{Numeric, alpha transparency for highlighted cells. Default is 1.}
}
\value{
A ggplot object.
}
\description{
This function creates scatter plots (e.g., for ADT features) highlighting a
specific population by displaying their density, similar to flow cytometry plots.
}
