% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_functions.R
\name{TpTnFpFn}
\alias{TpTnFpFn}
\title{Calculate True Positives, True Negatives, False Positives, and False Negatives}
\usage{
TpTnFpFn(x, y)
}
\arguments{
\item{x}{A logical vector (e.g., \code{colnames(so) \%in\% gate_list[["cl2"]]}).}

\item{y}{A logical vector (e.g., \code{so$is_cl2}).}
}
\value{
A named numeric vector with counts for tp, tn, fp, fn.
}
\description{
This helper function calculates the counts of TP, TN, FP, and FN given two
binary vectors (e.g., one from a gating strategy and one from a true cluster).
}
\examples{
tp_tn_fp_fn_example <- function() {
  set.seed(123)
  true_status <- sample(c(TRUE, FALSE), 100, replace = TRUE, prob = c(0.3, 0.7))
  gate_result <- sample(c(TRUE, FALSE), 100, replace = TRUE, prob = c(0.25, 0.75))
  TpTnFpFn(gate_result, true_status)
}
tp_tn_fp_fn_example()
}
